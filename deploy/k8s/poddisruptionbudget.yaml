apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ironwaves-backend
  labels:
    app: ironwaves-backend
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: ironwaves-backend

