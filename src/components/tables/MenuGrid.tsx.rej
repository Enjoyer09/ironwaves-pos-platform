@@ -135,6 +135,14 @@
     [items, selectedCategory, search, hideImages],
   );
 
+  const toggleImageVisibility = () => {
+    tapFeedback();
+    const next = !hideImages;
+    setHideImages(next);
+    if (typeof window !== 'undefined') {
+      localStorage.setItem('pos_hide_images', String(next));
+    }
+  };
 
   return (
     <div className="flex flex-col gap-3">
