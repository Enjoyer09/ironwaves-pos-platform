# 2026-08-21 Work Log

## Dashboard Full Audit (müştəri loyalty + menecer paneli)
- User: "dashboard bölməsi faydasız, qlobal brend proqramları daha yaxşı UI/UX; biz nə ilə geridəyik?" → audit istədi (hər ikisi: müştəri loyalty + menecer Dashboard).
- Əsas kod: `src/components/customer/HomeTab.tsx`, `ProfileTab.tsx`, `src/components/admin/DashboardPanel.tsx`.
- Müştəri loyalty indi: membership card (stamps/stars/tier/progress), wallet cashback, rewards, profile, notifications, purchase chart, order history, QR, wallet pass URL.
- Menecer Dashboard indi: critical alert bar (actions), 10 KPI, live feed, top products, staff perf, insights, date-range filter, lazy recharts, auto-refresh.
- Əsas boşluqlar: Müştəri — "növbəti mükafata yaxınlıq" aydın deyil, doğum günü/sürpriz mükafat surfaca çıxmayıb, personalizasiya/oyunlaşdırma yox, Wallet-ə bir toxunuş yox, zəif next-action. Menecer — keçən dövrə müqayisə (±%) yox, gündəlik hədəf ringi yox, export/drill-down zəif, əmək vs satış + müştəri analitikası yox, mobil dashboard zəif.
- Çıxış: `docs/DASHBOARD_AUDIT.md` yaradıldı (boşluq cədvəlləri + P0/P1/P2 prioritizasiya + 4 quick-win).
- User hələ implement istəməyib — yalnız audit. Növbəti addım: təsdiq etsə P0 quick-win-ləri (ana ekran "2 ulduz → pulsuz latte" pilli + doğum günü kartı + bir toxunuşlu Wallet + KPI ±% delta) implement edərik.

## P0 Quick-Win implementasiyası (user "bashla" dedi)
Audit təsdiqləndi → 4 quick-win kodu implement edildi və `npm run build` + `tsc --noEmit` uğurlu (0 xəta).
1. **HomeTab.tsx** — "Sənin üçün: X ulduz qaldı → pulsuz Latte" pilli (dinamik `wallet.stars_balance`/`next_reward_at`) + doğum günü kartı (`customer.birth_date`, 7 gün ərzində `daysUntilNextBirthday` ilə). Hər ikisi axtarış çubuğu ilə aktiv sifariş pilli arasına əlavə edildi.
2. **HomeTab.tsx header** — bir toxunuşlu "Wallet-ə əlavə et" düyməsi (mövcud `openWalletPass` + `get_customer_wallet_pass_url_fn` ilə), profil avatarı qabağına. Wallet pass bölməsi yerində qaldı.
3. **DashboardPanel.tsx** — KPI kartlarına "▲12% / ▼X%" delta (əvvəlki dövrün `get_sales_summary_live` + `get_period_expenses_summary` fetch-i ilə; revenue və expenses kartlarında). `prevSummary`/`prevExpenses` state + ayrı useEffect.
4. **DashboardPanel.tsx** — gündəlik satış hədəfi: `dailyGoal` localStorage-da (default 1000 ₼), redaktor input-u KPI bölməsinin yuxarısında; revenue kartında SVG progress ring (günə/aralığa uyğun `goalForRange`).
- Qeyd: Wallet düyməsi yalnız HomeTab header-ə qoyuldu (qlobal hər-ekran üçün hər tab-ın öz header-ı var; lazım olsa genişləndərik).
