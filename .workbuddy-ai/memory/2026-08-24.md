# 2026-08-24 Work Log

## P1 Quick-Win implementasiyası (audit `docs/DASHBOARD_AUDIT.md` əsasında)
User "Növbəti addım P1 kodla" dedi → P1 maddələrinin frontend-dən mümkün olanları koda yazıldı. `tsc --noEmit` + `npm run build` uğurlu (0 xəta).

### Müştəri (HomeTab.tsx)
- **Barista tövsiyəsi** kartı (personalizasiya): vaxta görə dəyişən tövsiyə, AI tab-na (`setActiveTab('ai')`) deep-link.
- **Günün sürprizi** oyunlaşdırma kartı: tap-to-reveal, gündə müəyyən motivasiya mesajı (deterministik).
- **"Hamısını təkrarla"** düyməsi "Sizin üçün" bölməsində: `recentItems` üzrə bir toxunuşla bütün son sifarişi kartə əlavə edir (`onReorderItem` + `reorderAll`).

### Menecer (DashboardPanel.tsx)
- **CSV export** düyməsi header-də: `snapshot.sales` → CSV yükləmə (BOM + ₼, tarix aralığı adında).
- **Saatlıq satış bölgüsü** PanelCard (drill-down): `snapshot.sales` timestamps → 24 saat üzrə barlar (`hourly` memo + `hourlyMax`).

### Deferred (backend lazımdır — implement edilmədi)
- **Əmək haqqı vs satış** (labor cost data yoxdur).
- **Top müştərilər** (customer aggregation endpoint yoxdur).
- Bunları etmək üçün backend-də uyğun məlumat sahələri əlavə olunmalı; user-ə bildirildi.

### Fayllar
- `src/components/customer/HomeTab.tsx`
- `src/components/admin/DashboardPanel.tsx`
- Yeni import: DashboardPanel-də `Download` (lucide).
