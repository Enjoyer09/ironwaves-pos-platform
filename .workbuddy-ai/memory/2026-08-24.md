# 2026-08-24 Work Log

## P1 Quick-Win implementasiyası (audit `docs/DASHBOARD_AUDIT.md` əsasında)
User "Növbəti addım P1 kodla" dedi → P1 maddələrinin frontend-dən mümkün olanları koda yazıldı. `tsc --noEmit` + `npm run build` uğurlu (0 xəta).

### Müştəri (HomeTab.tsx)
- **Barista tövsiyəsi** kartı (personalizasiya): vaxta görə dəyişən tövsiyə, AI tab-na (`setActiveTab('ai')`) deep-link.
- **Günün sürprizi** oyunlaşdırma kartı: tap-to-reveal, gündə müəyyən motivasiya mesajı (deterministik).
- **"Hamısını təkrarla"** düyməsi "Sizin üçün" bölməsində: `recentItems` üzrə bir toxunuşla bütün son sifarişi kartə əlavə edir (`onReorderItem` + `reorderAll`).

### Menecer (DashboardPanel.tsx)
- **CSV export** düyməsi header-də: `snapshot.sales` → CSV yükləmə (BOM + ₼, tarix aralığı adında).
- **Saatlıq satış bölgüsü** PanelCard (drill-down): `snapshot.sales` timestamps → 24 saat üzrə barlar (`hourly` memo + `hourlyMax`).

### Deferred (backend lazımdır — implement edilmədi)
- **Əmək haqqı vs satış** (labor cost data yoxdur).
- **Top müştərilər** (customer aggregation endpoint yoxdur).
- Bunları etmək üçün backend-də uyğun məlumat sahələri əlavə olunmalı; user-ə bildirildi.

### Fayllar
- `src/components/customer/HomeTab.tsx`
- `src/components/admin/DashboardPanel.tsx`
- Yeni import: DashboardPanel-də `Download` (lucide).

## Merchant Dashboard — UI/UX Audit (vizual qarışıqlıq)
- User: "dashboard ümumen qarışıq, estetik olaraq yorucu və çətindir" → tam UI/UX audit istədi.
- Audited `DashboardPanel.tsx` (kod səviyyəsində). Əsas qüsurlar:
  - **İerarxiya yoxdur**: 10 KPI + ayrı tam AnalyticsCenter (lazy) + 8 panel + 2 AI bölməsi (cyan+fuchsia) + alert bar hamısı eyni scroll-da, eyni çəki. 3-4 viewport həcmi.
  - **Rəng qarışıqlığı**: 9+ aksent rəngi (fuchsia/cyan/rose/emerald/amber/sky/violet/orange/yellow).
  - **Ağır tretman**: təkrar `shadow-[0_24px_80px...]` böyük kölgələr; arxada `web-aurora-subtle`+`web-noise-overlay` (customer app-də silinmişdi, admin-də qalır).
  - **Ardıcıl olmayan kart sistemləri**: PanelCard / KpiCard / ControlPanel / AI strips / AlertBar fərqli stillərdə.
  - **Oxunaqlılıq**: çoxlu text-[10-11px] etiket, aşağı kontrast (slate-500 üzərində).
  - **Təkrar metriklər**: revenue / avg ticket / COGS / open checks bir neçə yerdə.
- Deliverable: `docs/DASHBOARD_UIUX_AUDIT.md` (P0: AnalyticsCenter-i axından çıxar + KPI-ni 6-ya endir + AI birləşdir; P1: palitra məhdudlaşdır + tək kölgə + aurora sil + PanelCard unifikasiya; P2: etiket/oxunaqlılıq).
- Visualizer ilə cari-vs-təklif struktur müqayisəsi göstərildi (Overview 1 ekran + tablar modeli).
- Növbəti addım user təsdiqinə qaldı (P0-ları kodla etmək).

## Merchant Dashboard — "normal səviyyə" təmizləmə (P0-lar kodla edildi)
User "ishe bashla, dashboardu normal seviyyeye qaldiraq" dedi → `docs/DASHBOARD_UIUX_AUDIT.md` P0 planı kodla tətbiq edildi. `tsc --noEmit` (0 xəta) + `npm run build` uğurlu (DashboardPanel chunk 44.34 kB).

### Edits (hamısı `src/components/admin/DashboardPanel.tsx`)
- **#45 AnalyticsCenter-i yığılabilən et**: əvəlki sessiyada `const [showAnalytics, setShowAnalytics]` + mərkəzi "Ətraflı analitika" toggle düyməsi əlavə olunmuşdu (Suspense fallback radius artıq 20px). Bu sessiyada task tamamlandı.
- **#46 KPI 10→6 + toggle**: `KPISection`-də `const [showAllKpis, setShowAllKpis]` əlavə olundu. 6 əsas KPI həmişə görünür (satış, xalis mənfəət, açıq hesablar, mətbəx yükü, kassa fərqi, aktiv masalar); qalan 4 (xərclər, orta çek, COGS, marja) "Daha çox göstərici" toggle arxasında. Grid `xl:grid-cols-6` oldu.
- **#47 İki AI bölməsini birləşdir**: `AIManagerStrip` (cyan) + `BackgroundAgentStrip` (fuchsia) silindi, yerinə tək `AIPanel` (violet/indigo aksent) gəldi. AI menecer tövsiyələri (severity rəngli kartlar) + arxa plan agenti təhlilləri (yığılabilən, "Oxundu" işarəsi) bir paneldə. Boşdursa `return null`.
- **#48 Vizualı sakitləşdir**:
  - `DashboardLayout`-dən `web-aurora-subtle` + `web-noise-overlay` div-ləri silindi.
  - Bütün ağır kölgələr tək unifikasiya edildi: `shadow-[0_10px_34px_rgba(0,0,0,0.28)]` (KpiCard, PanelCard, AlertBar, hero, AIPanel).
  - `rounded-[28px]`/`[24px]` → `rounded-[20px]` (hero, sticky header `rounded-b-[20px]`, KpiCard, PanelCard, AlertBar, AIPanel).
  - Hero eyebrow `yellow-300` → `orange-400` (brand).

### Nəticə
Admin dashboard indi: alert bar → hero → 6 KPI (+toggle) → tək AI paneli → "Ətraflı analitika" toggle → əsas panellər (canlı satış, top məhsul, açıq hesablar, saatlıq, əmək vs satış / heyət, top müştəri, xəbərdarlıq). Aurora/noise + cyan/fuchsia + 10 KPI yox oldu. P1 (palitra məhdudlaşdırma, etiket/oxunaqlılıq) hələ edilməyib.

## Merchant Dashboard — P1 təmizləmə (palitra + oxunaqlılıq)
User "P1 davam et" dedi → `docs/DASHBOARD_UIUX_AUDIT.md` P1 qalan hissəsi kodla edildi. `tsc --noEmit` (0 xəta) + `npm run build` uğurlu.

### P1-A Palitra məhdudlaşdır
- KPISection-də "Aktiv masalar" KPI tonu `sky` (dekorativ mavi) → `slate` (neytral). İndi KPI tonları: emerald / violet / amber / rose / slate (5 semantik hüe). Mavi və cyan/fuchsia silindi.
- `AIPanel` "opportunity" severiti artıq violet istifadə edir (birləşmədən sonra tək aksent).

### P1-B Etiket oxunaqlılığı
- Bütün `text-slate-500` → `text-slate-400` (dashboard üzrə tək replace_all; kiçik etiketlərdə kontrast qalxdı).
- `text-[10px]` → `text-[11px]`: saatlıq bar vaxt/₼ etiketləri.
- "Göstəricilər" KPI eyebrow `opacity-50` → `text-slate-400` (çox solğu idi).
- TopProducts mətni `text-[11px]` → `text-xs` (12px).
- Qalan 2 `text-[10px]` (AI "yeni" rozetkası ağ mətnlə violet üzərində, və Top müştəri type pill-i slate-300) yüksək kontrastlı olduğu üçün toxunulmadı.

### Qalan (P2)
- "Overview 1 ekran + tablar" struktur işi (Analitika/Əmək/Müştərilər/Heyət/AI) — daha böyük refaktor.
