# 2026-08-20 Work Log

## Customer App — P2 Polish Fixes Implemented
- **C12 (aurora+noise bg)**: already implemented in a prior step — `customer-app-aurora` (animated `custAuroraDrift`, 22s/28s, reduced-motion respected) + `customer-app-noise` overlay in `src/index.css`; wired into `CustomerApp.tsx` background layer (dark mode). No new code needed.
- **C10 (scan-to-pay guidance)**: `HomeTab.tsx` QR Card now shows helper line "Kasada ödəmək üçün QR-kodu skan edin" (trilingual). Also covered in onboarding slide 3.
- **C11 (3-slide onboarding)**: `CustomerApp.tsx` — first-launch overlay gated by `localStorage['ironwaves_customer_onboarded']`. Module-level `ONBOARD_SLIDES` (TriLang object form {az,ru,en}) with 3 slides: Order ahead / AI Barista+Falçı / Scan at counter. Skip + Next/Get Started, page dots, scaleIn animation, `role="dialog" aria-modal`.
- `CUSTOMER_APP_FUNCTIONAL_AUDIT.md` P2 section marked ✅ HƏLL OLUNDU (theme-toggle-button visibility noted as remaining minor polish).
- `tsc --noEmit` → 0 errors. (Fixed TS7015 by switching ONBOARD_SLIDES title/body from tuple to `{az,ru,en}` object + `safeLang as 'az'|'ru'|'en'` cast.)
- **Status:** All P0 + P1 + P2 (C1–C12) customer-app audit findings now resolved. Only minor extra polish (visible theme toggle button) remains.

## Customer App — Cozy "Indie Café" Redesign (user feedback: "doesn't feel like a coffee shop")
- User chose **İsti & cozy (indie kafe)** direction.
- `customer.html`: added Fraunces cozy display serif (Google Fonts) alongside Nunito.
- `src/index.css`: added `.font-cozy` (Fraunces), `@keyframes steamRise` + `.coffee-steam` rising-wisp animation (reduced-motion respected); warmed `.customer-app-shell` bg to espresso `#1A120B→#0f0a06`.
- `CustomerApp.tsx`: warmed main background gradient to espresso (`#2A1A10→#160D07`) dark + cream (`#FCF4EA→#F2E4D2`) light.
- `HomeTab.tsx`: new `CoffeeCupSteam` component (SVG cup + latte-art rosette + 3 steam wisps); `coffeeGreeting()` time-based greeting; Hero Greeting reworked — time greeting + name + "Bugün hansı qəhvə ilə başlayırıq?" in `font-cozy` + barista tip line + steam cup; membership card title also `font-cozy`.
- `tsc --noEmit` → 0 errors.
- Note: brand accent stayed #F48C24 (reads as caramel/warm). Functional behavior unchanged.

## Logo wired — Emalathhana as Customer App brand
- User provided `/Users/macbookair/Desktop/loqo.jpg` (EMALATHHANA • Daily Coffee & Drinks • SINCE 2020, orange bg) and asked to make it the app's logo.
- Copied to `public/logo.jpg` (Vite static asset; original Desktop file untouched).
- `customer.html`: favicon + apple-touch-icon → `/logo.jpg`; theme-color → `#F48C24` (brand orange); `<title>` → `Emalathhana`.
- `HomeTab.tsx`: centered brand mark (h-9 w-9 rounded-xl object-cover) in the top header between Menu and avatar.
- `CustomerApp.tsx` (onboarding): logo h-14 w-14 at top of the welcome card.
- `OrderTab.tsx` (PreOrderSuccess): logo h-14 w-14 above the 🎉 on the order-confirmed screen.
- `tsc --noEmit` → 0 errors.
- Note: iRonWaves is the platform; Emalathhana is the tenant brand — logo now represents the tenant. Original Desktop file left untouched (read-only copy).

## Logo → full App Icon (PWA + iOS + Android native)
- User request: "logo faylini proqramin ikonu et" (make the logo the app's icon everywhere).
- Source `public/logo.jpg` is only 150×150 (low-res JPG) — upscaled for all targets (soft but asset-limited).
- Generator: `scripts/gen_icons.py` (Python + Pillow, managed venv). Sampled brand orange from source corner = **#FF8B26** (used for seamless maskable/adaptive fills; differs from app accent #F48C24).
- Generated: `public/logo-192.png` (192², purpose any), `public/logo-512.png` (512², any), `public/logo-512-maskable.png` (512², orange field + logo within 80% safe zone, purpose "any maskable").
- iOS: overwrote `ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png` at 1024² (Xcode downscales).
- Android legacy: overwrote `ic_launcher.png` + `ic_launcher_round.png` at 48/72/96/144/192 (mdpi→xxxhdpi) — full-bleed logo.
- Android adaptive fg: overwrote `ic_launcher_foreground.png` at 108/162/216/324/432, logo scaled into 66% safe zone on transparent canvas.
- `vite.config.ts` VitePWA manifest: name/short_name "Emalathhana", theme_color #F48C24, background_color #160D07, categories food/business, icons → the 3 logo PNGs (any / any / any maskable). Removed old `vite.svg` icon.
- Android adaptive bg color `values/ic_launcher_background.xml` → #FF8B26; `drawable/ic_launcher_background.xml` simplified to solid #FF8B26 (removed old teal grid).
- `tsc --noEmit` → 0 errors. To see changes, rebuild (`npm run build:customer` / `cap sync`) — native icons need a fresh native build to take effect.
